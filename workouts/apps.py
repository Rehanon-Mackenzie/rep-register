from django.apps import AppConfig


class WorkoutsConfig(AppConfig):
    """
    Configuration class for the workouts app.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'workouts'
